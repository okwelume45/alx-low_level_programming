#include "main.h"
#include <string.h>

/**
 * _strcmp -> for comparison
 * @s1: first arg
 * @s2: second arg
 * Return: integer
 */

int _strcmp(char *s1, char *s2)
{
	int res;

	res = strcmp(s1, s2);
	return (res);
}
